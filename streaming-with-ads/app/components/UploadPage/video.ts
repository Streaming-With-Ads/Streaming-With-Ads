export interface Video {
    id: number;
    title: string;
    description: string;
    thumbnail: string;
  }
  
  export const initialVideos: Video[] = [
    // You can initialize with some default videos if needed
  ];
  